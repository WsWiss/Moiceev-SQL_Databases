{
  "name": "shop-site",
  "version": "1.0.0",
  "scripts": {
    "init": "node server/init-db.js",
    "start": "node server/server.js",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:start": "docker-compose up -d && timeout /t 3 /nobreak >nul && npm run init && npm start"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.3"
  }
}